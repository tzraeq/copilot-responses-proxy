[package]
name = "httpdate"
version = "1.0.3"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Vendored minimal httpdate compatibility crate for offline builds"

[lib]
path = "src/lib.rs"
